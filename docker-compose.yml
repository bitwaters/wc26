services:
  app:
    build: .
    restart: unless-stopped
    env_file:
      - .env.local
    environment:
      - NODE_ENV=production
    ports:
      - "3000:3000"
    volumes:
      - wc26_data:/app/data

volumes:
  wc26_data:
    driver: local
