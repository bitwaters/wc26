# Copy to .env.local and restart the dev server:
#   cp .env.example .env.local
#
# Protects write API routes (bets, predictions, reset, etc.).
# When set, also enter the same value in Settings → 本地 API 令牌.
# Leave unset for open local development without auth.
LOCAL_API_SECRET=
